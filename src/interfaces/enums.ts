export enum PAYMENT_RESPONSE {
  ACCEPT = 'accept',
  DECLINED = 'declined'
}

export enum ORDER_STATUS {
  WAITING = 'waiting',
  PREPARING = 'preparing',
  READY = 'ready',
  DELIVERED = 'delivered',
  DONE = 'done'
}